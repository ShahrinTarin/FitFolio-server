apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitfolio.fullname" . }}
  labels:
    {{- include "fitfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "fitfolio.selectorLabels" . | nindent 4 }}
